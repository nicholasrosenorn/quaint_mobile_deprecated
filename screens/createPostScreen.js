import { Text } from 'react-native';

const CreatePostScreen = ({navigation, route}) => {
    return <Text style = {{ paddingTop: 50 }}>This is the create post screen</Text>;
  };

export default CreatePostScreen;