import { Text } from 'react-native';

const JourneyScreen = ({navigation, route}) => {
    return <Text style = {{ paddingTop: 50 }}>This is the journey screen</Text>;
  };

export default JourneyScreen;